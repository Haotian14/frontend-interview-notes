import type { TopicPractice } from '../../../types';

export const practice: TopicPractice = {
  slug: 'structural-variance',
  code: {
    input: '把一个含额外属性的对象字面量直接赋给窄接口，再把同一个对象先赋给变量后再赋值，比较两次的报错。',
    output: '直接写字面量会触发多余属性检查而报错，先存进变量就不报错——因为多余属性检查只针对"新鲜"的对象字面量，它是一层额外的启发式保护，而不是类型系统的赋值规则。理解这一点才能解释很多"为什么这里报错那里不报错"。',
  },
  reference: {
    caption: '型变规则速查',
    rows: [
      { term: '对象属性', meaning: '协变：属性类型是子类型则整体是子类型。' },
      { term: '函数返回值', meaning: '协变：返回更具体的类型是安全的。' },
      { term: '函数参数（strictFunctionTypes 开启）', meaning: '逆变：接受更宽的参数才是子类型。' },
      { term: '方法简写语法', meaning: '始终双变，是为兼容数组等内置 API 保留的不安全妥协。' },
      { term: '数组与可变容器', meaning: 'TypeScript 按协变处理，理论上不安全，需靠 readonly 规避。' },
      { term: 'readonly T[]', meaning: '只读容器的协变是安全的，能接受更多实参类型。' },
    ],
  },
  interview: {
    answer: 'TypeScript 的类型兼容是结构化的：只看形状不看声明来源，只要属性齐全且类型兼容就可以赋值，这也是它能和 JavaScript 生态无缝配合的原因。由此带来两个常被问到的点。第一是多余属性检查：直接写对象字面量赋值时，多出来的属性会报错，但把同一个对象先存进变量再赋值就不报错，因为多余属性检查只针对新鲜的字面量，是一层额外的启发式保护而不是赋值规则本身。第二是型变。对象属性和函数返回值是协变的，也就是子类型可以替换父类型；函数参数在开启 strictFunctionTypes 后是逆变的，即接受更宽参数的函数才是子类型，因为它能应对更多调用；但用方法简写语法声明的函数始终是双变的，这是为了兼容数组 API 保留的不安全妥协。另外数组在 TypeScript 里被当作协变处理，把子类型数组赋给父类型数组后再往里写入就会破坏类型，实践中用 readonly 数组来避免。工程上如果需要名义类型语义，比如区分用户 ID 和订单 ID，可以用带 unique symbol 的品牌类型模拟。',
    followUps: [
      '为什么把对象字面量直接赋值会报多余属性，先赋给变量就不会？',
      '函数参数为什么应该是逆变的，方法简写为什么是双变？',
      '数组的协变为什么不安全，怎么规避？',
    ],
  },
};
