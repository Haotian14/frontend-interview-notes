import type { TopicPractice } from '../../../types';

export const practice: TopicPractice = {
  slug: 'http-versions',
  code: {
    input: '在 DevTools 的 Network 面板打开 Protocol 列，对比同一站点在 HTTP/1.1 与 HTTP/2 下加载几十个小资源的瀑布图。',
    output: 'HTTP/1.1 下同域并发连接数受限，请求排队形成明显的阶梯；HTTP/2 下所有请求复用一条连接几乎同时开始。这直接解释了为什么合并雪碧图、域名分片这些老优化在 HTTP/2 之后反而有害。',
  },
  reference: {
    caption: '三个版本的关键差异',
    rows: [
      { term: 'HTTP/1.1', meaning: '文本协议，一条连接同时只处理一个请求，靠多开连接提高并发。' },
      { term: 'HTTP/2', meaning: '二进制分帧与多路复用，解决应用层队头阻塞，头部用 HPACK 压缩。' },
      { term: 'HTTP/2 的残留问题', meaning: '仍跑在 TCP 上，丢包会阻塞该连接上的所有流。' },
      { term: 'HTTP/3', meaning: '基于 QUIC 与 UDP，流之间真正独立，握手更快并支持连接迁移。' },
      { term: '被淘汰的优化', meaning: '域名分片、雪碧图、内联小资源、合并文件在多路复用下收益转负。' },
      { term: '仍然有效的优化', meaning: '减少字节数、提前建连、正确的优先级与预加载。' },
    ],
  },
  interview: {
    answer: '这条演进线的主题是消除队头阻塞。HTTP/1.1 一条连接同一时刻只能处理一个请求，后面的必须排队，浏览器只好对每个域名开六到八条连接，于是产生了域名分片、雪碧图、合并文件这些绕开并发限制的技巧。HTTP/2 引入二进制分帧和多路复用，同一条连接上可以有很多个流并行，头部用 HPACK 压缩，应用层的队头阻塞就没有了，前面那些技巧也随之失效甚至有害，因为合并会破坏缓存粒度、分片会增加握手成本。但 HTTP/2 仍然跑在 TCP 上，一旦发生丢包，TCP 为了保证有序交付会阻塞这条连接上的所有流，这是传输层的队头阻塞。HTTP/3 把传输层换成基于 UDP 的 QUIC，流之间真正独立，一个流丢包不影响其它流，同时把 TLS 握手合进传输握手，建连更快，还支持连接迁移让切换网络时不断线。对前端的实际影响是：优化重点从减少请求数转向减少字节数和正确安排优先级，用 preconnect 提前建连、用 preload 和 fetchpriority 表达重要性、用合理的分包让缓存命中率更高。',
    followUps: [
      '为什么 HTTP/2 之后不再推荐雪碧图和域名分片？',
      'HTTP/2 的多路复用为什么没有彻底解决队头阻塞？',
      'HTTP/3 相比 HTTP/2 在移动端有什么实际收益？',
    ],
  },
};
