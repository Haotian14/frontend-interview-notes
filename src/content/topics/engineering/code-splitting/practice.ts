import type { TopicPractice } from '../../../types';

export const practice: TopicPractice = {
  slug: 'code-splitting',
  code: {
    input: '把一个只有点击"导出"按钮才用到的表格导出库改成在事件处理器里 `await import("./exporter")`，构建后对比首屏 chunk 体积。',
    output: '该库从首屏 chunk 中消失，变成一个独立文件，只有用户真正点击时才下载。若在按钮 hover 时提前触发同一个 import，点击后的等待也几乎消失——说明分割的代价是延迟，而预取正是用来抵消它的。',
  },
  reference: {
    caption: '三条分割线与配套手段',
    rows: [
      { term: '路由级分割', meaning: '收益最大最安全的默认策略，每个页面一个 chunk。' },
      { term: '交互级分割', meaning: '弹窗、编辑器、图表等点击后才用的重组件。' },
      { term: '可见性分割', meaning: '折叠区域、下方模块用 IntersectionObserver 触发加载。' },
      { term: '预取', meaning: 'hover、空闲时或路由预测时提前下载，抵消点击后的等待。' },
      { term: '加载状态', meaning: '用骨架屏保持布局稳定，避免分割引入新的 CLS。' },
      { term: '失败重试', meaning: '发布后旧 chunk 可能 404，需要捕获并提示刷新。' },
    ],
  },
  interview: {
    answer: '代码分割的目的是让首屏只下载和解析真正需要的代码，语法基础是动态 import，它返回 Promise 并天然成为打包器的分割点。我按三条线切：第一条是路由级，每个页面一个 chunk，这是收益最大也最安全的默认策略；第二条是交互级，弹窗、富文本编辑器、图表这类点击后才用到的重组件；第三条是可见性，折叠区域或首屏之下的模块用 IntersectionObserver 触发加载。但分割不是免费的，它把体积问题换成了延迟问题，所以要配三样东西。一是预取，在 hover、浏览器空闲或能预测下一步时提前下载，让用户感觉不到等待；二是加载状态，用尺寸稳定的骨架屏，否则会引入新的布局偏移；三是失败处理，发布之后旧的 chunk 文件可能已经不存在，动态 import 会失败，需要捕获并提示用户刷新。粒度上也要克制，切得太碎会产生请求瀑布和重复的公共代码，通常路由加上少数几个重组件就够了，剩下的交给打包器的自动分包，然后用产物分析验证效果。',
    followUps: [
      '代码分割会带来哪些新问题，怎么缓解？',
      '什么时候应该预取，怎么实现？',
      '为什么发布后会出现动态 import 加载失败？',
    ],
  },
};
