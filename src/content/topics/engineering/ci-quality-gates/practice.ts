import type { TopicPractice } from '../../../types';

export const practice: TopicPractice = {
  slug: 'ci-quality-gates',
  code: {
    input: '在流水线里把 lint、类型检查、单元测试、构建拆成并行任务，并缓存依赖目录，与串行且不缓存的版本对比总耗时。',
    output: '并行加缓存通常能把十分钟级的流水线压到两三分钟。更重要的是失败反馈更早：类型错误在一分钟内就能报出来，不必等完整测试跑完。这说明闸门的价值不只是拦住问题，还在于反馈速度。',
  },
  reference: {
    caption: '一条实用的前端流水线',
    rows: [
      { term: '1. 安装与缓存', meaning: '锁文件驱动的确定性安装，缓存依赖目录。' },
      { term: '2. 静态检查', meaning: 'lint 与格式化检查，最快失败，先跑。' },
      { term: '3. 类型检查', meaning: 'tsc --noEmit，覆盖测试跑不到的组合。' },
      { term: '4. 单元与组件测试', meaning: '并行分片，产出覆盖率但不把覆盖率当目标。' },
      { term: '5. 构建与体积预算', meaning: '产物必须能构建成功，且体积不超过阈值。' },
      { term: '6. 端到端与可访问性', meaning: '在预览环境跑关键路径，数量少而稳。' },
    ],
  },
  interview: {
    answer: '我把 CI 看成一组按成本从低到高排列的闸门，越便宜的越早跑，让失败反馈尽可能快。顺序一般是：确定性安装并缓存依赖，然后 lint 和格式检查，接着类型检查，再是单元和组件测试，然后构建并检查产物体积预算，最后在预览环境跑少量端到端和可访问性用例。这些任务能并行的都并行，测试按文件分片，一条前端流水线控制在几分钟内是合理目标，超过十分钟大家就会开始绕过它。闸门本身要有明确的失败标准，比如体积超预算直接失败，而不是打印一行警告让人忽略。发布侧我关注三件事：一是灰度，先放一小部分流量并观察错误率和核心指标，再逐步放大；二是特性开关，让发布和启用解耦，出问题可以关掉开关而不必回滚整个版本；三是回滚要快且演练过，静态资源保留旧版本，HTML 不缓存，这样回滚才是秒级的。最后要把监控数据接进这个闭环，发布事件标注在错误率曲线上，异常时能立刻定位到是哪个版本引入的。',
    followUps: [
      '质量闸门应该按什么顺序排列，为什么？',
      '灰度发布和特性开关分别解决什么问题？',
      '为什么不应该把测试覆盖率当作目标？',
    ],
  },
};
