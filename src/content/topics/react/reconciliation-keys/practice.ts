import type { TopicPractice } from '../../../types';

export const practice: TopicPractice = {
  slug: 'reconciliation-keys',
  code: {
    input: '渲染一个可删除的输入框列表，用数组索引作为 key，在第一个输入框里打字后删除它，观察剩下输入框里的内容。',
    output: '被删除项的输入内容会"跳"到后一项上，因为索引没变而对应的数据变了，React 认为是同一个组件从而复用了它的内部状态。换成稳定的数据 id 作 key 后行为正确，这说明 key 的职责是标识身份，不是消除警告。',
  },
  reference: {
    caption: '状态保留与重置的判定',
    rows: [
      { term: '同位置同类型', meaning: '复用组件实例与其内部状态，只更新 props。' },
      { term: '同位置不同类型', meaning: '卸载旧子树、重建新子树，状态全部丢失。' },
      { term: '不同 key', meaning: '视为不同组件，强制重建，是主动重置状态的手段。' },
      { term: '索引作 key', meaning: '列表可增删或排序时会把状态错配到相邻项。' },
      { term: '随机数作 key', meaning: '每次渲染都重建，状态永远丢失且性能最差。' },
      { term: '条件渲染两个分支', meaning: '同位置同类型会复用，需要重置就给不同 key。' },
    ],
  },
  interview: {
    answer: 'React 的协调基于两个假设来把 O(n 的三次方) 的树 diff 降到 O(n)：不同类型的元素会产出不同的树，以及开发者可以用 key 标记哪些子元素在多次渲染中是稳定的。具体规则是按同层的位置比较：位置和元素类型都相同就复用同一个实例，保留它的 state 和 DOM 节点，只更新 props；类型不同就卸载整棵旧子树再重建，状态全部丢失。key 的作用是在同一层里给元素一个身份标识，让 React 在顺序变化时知道谁是谁。所以用数组索引作 key 在静态列表里没问题，一旦列表可以增删或排序，索引和数据的对应关系就会错位，表现为输入框内容、勾选状态跳到相邻项上。key 只需要在兄弟节点之间唯一，不需要全局唯一，也不会作为 prop 传给组件。反过来，key 也是主动重置状态的正规手段：给组件换一个 key 就能让 React 卸载重建，比写 effect 去同步重置干净得多。',
    followUps: [
      '为什么用索引作 key 会导致状态错乱？',
      'key 需要全局唯一吗，能不能在组件里读到它？',
      '怎么用 key 主动重置一个组件的状态？',
    ],
  },
};
