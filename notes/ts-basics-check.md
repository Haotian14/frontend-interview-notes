# TS Basics Check（wk0 day2 / wk1 day1）

> 用法：每个点包含 3 样东西：
>
> 1. ✅🟡🔴 2) 面试一句话解释 3) 最小例子/关键词  
>    ✅ 讲得清 + 写得出；🟡 讲得清但写不顺/容易错；🔴 不清楚或没做过。

---

## 0. 现状标记

- 今日日期：2026-01-14
- 当前自评：🔴（整体）
- 本次目标：把每一项都补上“面试一句话 + 最小例子/关键词”，每天把 🔴→🟡→✅

---

## 1) 基础类型与语法

- 基础类型（string/number/boolean/null/undefined/symbol/bigint）：🔴

  - 一句话：TS 在 JS 基础上增加静态类型，常见基础类型用于描述变量/参数/返回值。
  - 关键词/例子：`let a: number = 1`

  ```ts
  let name: string = "hart";
  let age: number = 20;
  let ok: boolean = true;
  ```

- any / unknown / never 区别：🔴

  - 一句话：any 放弃类型检查；unknown 安全的“未知”（要收窄才能用）；never 表示“不可能发生”（如抛错/死循环）。
  - 关键词：unknown 需要收窄、never 用于穷尽检查

  ```ts
  let a: any = 1;
  a.foo();
  let u: unknown = 1; // u.foo() ❌
  function fail(): never {
    throw new Error("x");
  }
  ```

- void 与函数返回值：🔴

  - 一句话：void 表示函数不返回有意义的值（通常 return undefined）。
  - 关键词：副作用函数

  ```ts
  function log(msg: string): void {
    console.log(msg);
  }
  ```

- 类型推断（inference）：🔴

  - 一句话：很多情况下 TS 能根据赋值/返回值自动推断类型，不必写注解。
  - 关键词：自动推断

  ```ts
  const n = 1; // 推断为 number
  const arr = [1, 2, 3]; // number[]
  ```

- 类型断言（as / <>）：🔴

  - 一句话：断言是告诉 TS“我比你更确定”，不会做运行时转换；能少用就少用。
  - 关键词：只在必要时断言

  ```ts
  const el = document.getElementById("app") as HTMLDivElement;
  ```

- 可选属性与可选链（? / ?.）：🔴

  - 一句话：可选属性表示可能不存在；可选链用于安全访问深层属性。
  - 关键词：`?:` `?.`

  ```ts
  type User = { name: string; age?: number };
  const age = user.age?.toFixed(0);
  ```

- 非空断言（!）：🔴

  - 一句话：告诉 TS “这里一定不是 null/undefined”，但可能埋雷（运行时仍可能为 null）。
  - 关键词：慎用

  ```ts
  const el = document.getElementById("app")!;
  ```

- 字面量类型（literal types）：🔴

  - 一句话：把取值限制在固定集合里，常用于枚举式状态。
  - 关键词：联合字面量

  ```ts
  type Size = "sm" | "md" | "lg";
  const s: Size = "md";
  ```

---

## 2) 联合 / 交叉 / 类型收窄

- union（|）联合类型：🔴

  - 一句话：变量可能是多种类型之一，使用前通常需要收窄。
  - 关键词：`|`

  ```ts
  function f(x: string | number) {
    return String(x);
  }
  ```

- intersection（&）交叉类型：🔴

  - 一句话：把多个类型“合并”成一个，同时拥有所有属性。
  - 关键词：对象组合

  ```ts
  type A = { a: number };
  type B = { b: string };
  type C = A & B;
  ```

- 类型收窄（typeof / in / instanceof）：🔴

  - 一句话：通过运行时检查把联合类型缩小到具体类型。
  - 关键词：type narrowing

  ```ts
  function f(x: string | number) {
    if (typeof x === "string") return x.toUpperCase();
    return x.toFixed(2);
  }
  ```

- 可辨识联合（discriminated union）：🔴

  - 一句话：用一个共同字段（tag）区分联合分支，是最推荐的建模方式。
  - 关键词：`kind/type` 字段

  ```ts
  type Shape = { kind: "circle"; r: number } | { kind: "square"; s: number };

  function area(sh: Shape) {
    if (sh.kind === "circle") return Math.PI * sh.r * sh.r;
    return sh.s * sh.s;
  }
  ```

- 自定义类型守卫（type predicate）：🔴

  - 一句话：用函数封装复杂判断，让 TS 在调用处自动收窄。
  - 关键词：`x is T`

  ```ts
  function isString(x: unknown): x is string {
    return typeof x === "string";
  }
  ```

---

## 3) Interface vs Type

- interface vs type 区别与使用场景：🔴

  - 一句话：interface 适合对象形状、可声明合并；type 更灵活（联合/交叉/别名）。
  - 关键词：对象用 interface，组合/联合用 type（常见经验）

  ```ts
  interface User {
    name: string;
  }
  type ID = string | number;
  ```

- interface 合并（declaration merging）：🔴

  - 一句话：同名 interface 会自动合并，常用于扩展第三方类型。
  - 关键词：声明合并

  ```ts
  interface Window {
    foo: string;
  }
  interface Window {
    bar: number;
  }
  // Window 现在有 foo + bar
  ```

- extends / 组合（composition）：🔴

  - 一句话：extends 表示继承/扩展；也可以用交叉类型做组合。
  - 关键词：复用字段

  ```ts
  interface A {
    a: number;
  }
  interface B extends A {
    b: string;
  }
  type C = A & { c: boolean };
  ```

- readonly / 可选属性：🔴

  - 一句话：readonly 禁止重新赋值；可选属性表示可能没有。
  - 关键词：只读/可选

  ```ts
  interface U {
    readonly id: string;
    nick?: string;
  }
  ```

- 索引签名（index signature）：🔴

  - 一句话：表示对象的 key/value 结构（字典类型）。
  - 关键词：Record 的底层思路

  ```ts
  type Dict = { [key: string]: number };
  ```

---

## 4) 泛型（Generics）

- 泛型函数 `<T>`:🔴

  - 一句话：用类型参数描述“输入输出的关系”，避免 any。
  - 关键词：保持类型信息

  ```ts
  function id<T>(x: T): T {
    return x;
  }
  ```

- 泛型约束 extends：🔴

  - 一句话：限制 T 必须满足某种结构，才能使用某些属性。
  - 关键词：约束

  ```ts
  function len<T extends { length: number }>(x: T) {
    return x.length;
  }
  ```

- 默认泛型参数：🔴

  - 一句话：给泛型提供默认类型，调用时可省略。
  - 关键词：默认值

  ```ts
  type Box<T = string> = { value: T };
  ```

- 多泛型参数（<T, K>）：🔴

  - 一句话：多个类型参数描述更复杂关系，比如 key/value。
  - 关键词：K extends keyof T

  ```ts
  function pick<T, K extends keyof T>(obj: T, key: K) {
    return obj[key];
  }
  ```

- 泛型在 React 中的常见用法：🔴

  - 一句话：useState/useRef/组件 props 常通过泛型表达更精确类型。
  - 关键词：`useState<T>()`

  ```ts
  const [n, setN] = useState<number>(0);
  const ref = useRef<HTMLDivElement | null>(null);
  ```

---

## 5) 函数与类型

- 函数类型声明（参数/返回值）：🔴

  - 一句话：给函数参数和返回值加类型，保证调用安全。
  - 关键词：函数签名

  ```ts
  function add(a: number, b: number): number {
    return a + b;
  }
  ```

- 可选参数与默认参数：🔴

  - 一句话：`x?: T` 表示可选；默认值参数天然可选。
  - 关键词：`?`

  ```ts
  function hi(name = "guest", age?: number) {}
  ```

- rest 参数与 tuple：🔴

  - 一句话：rest 可以用数组或 tuple 精确描述参数列表。
  - 关键词：`...args: number[]`

  ```ts
  function sum(...nums: number[]) {
    return nums.reduce((a, b) => a + b, 0);
  }
  ```

- 函数重载（overloads）：🔴

  - 一句话：一个函数多种入参形式，用重载签名描述返回值差异。
  - 关键词：多个签名 + 一个实现

  ```ts
  function toArr(x: number): number[];
  function toArr(x: string): string[];
  function toArr(x: any) {
    return [x];
  }
  ```

- this 参数类型（this: XXX）：🔴

  - 一句话：显式声明 this 类型，避免回调里 this 乱指。
  - 关键词：TS 特性

  ```ts
  function f(this: { x: number }) {
    return this.x;
  }
  ```

---

## 6) 对象、数组、Tuple

- 对象类型与结构化类型系统：🔴

  - 一句话：TS 是结构化类型，只看“形状”是否兼容，而不是名字。
  - 关键词：duck typing

  ```ts
  type A = { x: number };
  const a: A = { x: 1, y: 2 }; // 多余属性看场景（字面量会报错）
  ```

- 数组类型 T[] vs Array`<T>`：🔴

  - 一句话：两者等价，风格不同；泛型写法更统一。
  - 关键词：等价

  ```ts
  const a: number[] = [1];
  const b: Array<number> = [1];
  ```

- tuple 基础：🔴

  - 一句话：tuple 表示“定长 + 每个位置类型固定”。
  - 关键词：位置类型

  ```ts
  const t: [string, number] = ["a", 1];
  ```

- readonly tuple：🔴

  - 一句话：只读 tuple 防止被修改。
  - 关键词：immutable

  ```ts
  const t: readonly [string, number] = ["a", 1];
  ```

---

## 7) Enum（了解即可）

- enum 基础与风险点：🔴

  - 一句话：enum 会生成运行时代码，可能带来体积/一致性问题，项目常用联合字面量替代。
  - 关键词：运行时产物

- const enum（了解）：🔴

  - 一句话：const enum 会被内联（但在某些编译配置下不推荐）。
  - 关键词：inline

---

## 8) 类型工具（Utility Types）

- Partial：🔴

  - 一句话：把所有属性变成可选。
  - 关键词：更新 patch

  ```ts
  type P = Partial<{ a: number; b: string }>;
  ```

- Required：🔴

  - 一句话：把所有属性变成必选。
  - 关键词：补全

- Readonly：🔴

  - 一句话：把所有属性变成只读。
  - 关键词：不可变

- Pick：🔴

  - 一句话：从类型里挑选一部分字段。
  - 关键词：子集

  ```ts
  type U = { id: string; name: string; age: number };
  type A = Pick<U, "id" | "name">;
  ```

- Omit：🔴

  - 一句话：从类型里排除一部分字段。
  - 关键词：排除

  ```ts
  type B = Omit<U, "age">;
  ```

- Record：🔴

  - 一句话：描述一个键值映射对象（字典）。
  - 关键词：map/dict

  ```ts
  type M = Record<string, number>;
  ```

- Exclude / Extract：🔴

  - 一句话：Exclude 从联合里去掉；Extract 从联合里保留。
  - 关键词：union 操作

  ```ts
  type E = Exclude<"a" | "b" | "c", "c">; // "a"|"b"
  type X = Extract<"a" | "b" | "c", "c" | "d">; // "c"
  ```

- NonNullable：🔴

  - 一句话：从类型里去掉 null/undefined。
  - 关键词：去空

- ReturnType / Parameters：🔴

  - 一句话：从函数类型中提取返回值类型/参数类型。
  - 关键词：类型推导

  ```ts
  type R = ReturnType<() => number>; // number
  type Pm = Parameters<(a: string, b: number) => void>; // [string, number]
  ```

---

## 9) 高级类型（面试加分，先了解）

- keyof：🔴

  - 一句话：取对象类型的所有 key，得到联合类型。
  - 关键词：键集合

  ```ts
  type K = keyof { a: number; b: string }; // "a" | "b"
  ```

- typeof（类型层面的）：🔴

  - 一句话：把值的类型拿出来复用（不是 JS 的 typeof）。
  - 关键词：从值到类型

  ```ts
  const cfg = { mode: "dev" };
  type Cfg = typeof cfg;
  ```

- 索引访问类型 T[K]：🔴

  - 一句话：用 key 访问对象类型某个字段的类型。
  - 关键词：取属性类型

  ```ts
  type U = { id: string; age: number };
  type Age = U["age"]; // number
  ```

- 条件类型（conditional types）：🔴

  - 一句话：类型层面的 if/else，用于根据条件生成类型。
  - 关键词：`T extends U ? X : Y`

- infer（了解）：🔴

  - 一句话：在条件类型里“推断”某一部分类型。
  - 关键词：类型抽取

- 映射类型（mapped types）：🔴

  - 一句话：遍历 key 来生成新类型（Partial/Readonly 底层思路）。
  - 关键词：`[K in keyof T]`

- 模板字面量类型（template literal types）：🔴

  - 一句话：用字符串模板拼接类型，常用于生成类名/事件名。
  - 关键词：`${}`

  ```ts
  type EventName = `on${Capitalize<"click" | "change">}`; // "onClick" | "onChange"
  ```

---

## 10) TS 工程化（实践必备）

- tsconfig 常见项（target/module/strict/esModuleInterop）：🔴

  - 一句话：tsconfig 控制编译目标、模块系统与严格程度，影响工程行为与兼容性。
  - 关键词：strictNullChecks、moduleResolution

- strict 相关（noImplicitAny / strictNullChecks）：🔴

  - 一句话：strictNullChecks 最关键，能防止大量运行时空值错误。
  - 关键词：`x?.y`、`x!`

- 类型声明文件 d.ts：🔴

  - 一句话：用 d.ts 给 JS/库提供类型信息，让 TS 能检查与提示。
  - 关键词：declare module

- 第三方库类型（@types）：🔴

  - 一句话：没有内置类型的库需要安装 `@types/xxx`。
  - 关键词：DefinitelyTyped

---

## 11) 与 React 相关（你会用就行）

- React 组件 props 类型：🔴

  - 一句话：用 interface/type 定义 props，组件入参解构时自动获得类型提示。
  - 关键词：Props

  ```ts
  type Props = { title: string; onClick?: () => void };
  function Card({ title, onClick }: Props) {
    return null;
  }
  ```

- 事件类型（ChangeEvent/MouseEvent）：🔴

  - 一句话：React 合成事件有自己的类型，用于更精确的 event.target。
  - 关键词：`React.ChangeEvent<HTMLInputElement>`

  ```ts
  const onChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    console.log(e.target.value);
  };
  ```

- useRef / useState 常见泛型写法：🔴

  - 一句话：useState/useRef 用泛型明确类型，避免 any 和 null 错误。
  - 关键词：`useRef<T | null>`

  ```ts
  const [n, setN] = useState<number>(0);
  const ref = useRef<HTMLDivElement | null>(null);
  ```

---

## 12) 今日红黄清单（用于明天计划）

- 🔴 TOP3：
  1.
  2.
  3.
- 🟡 TOP3：
  1.
  2.
  3.

---

## 13) 备注（自己的坑/易错点）

-
